# TAMS Frontend Environment Variables
# Live Backend Configuration

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================
# Choose which backend to use by setting VITE_DEFAULT_BACKEND
# Available options: vast-tams, bbc-tams-demo, ibc-thiago, ibc-thiago-imported, upcoming-demo

# CURRENT BACKEND (ACTIVE) - IBC Thiago with Imported Data
#VITE_DEFAULT_BACKEND=ibc-thiago-imported

# PREVIOUS BACKEND (COMMENTED OUT) - VAST TAMS production backend
VITE_DEFAULT_BACKEND=vast-tams

# =============================================================================
# BACKEND API URLs
# =============================================================================

# VAST TAMS Backend (Full-featured production backend)
# Note: This backend automatically uses a proxy to avoid CORS issues
VITE_BACKEND_VAST_TAMS_URL=http://34.216.9.25:8000

# BBC TAMS Demo Backend (BBC specification compliance demo)
VITE_BACKEND_BBC_DEMO_URL=https://bbc-tams-demo.example.com

# IBC Thiago Backend (BBC TAMS v3 demo with HLS streaming and real-time markers)
# This is the new backend for Phase 8 integration
VITE_BACKEND_IBC_THIAGO_URL=http://localhost:3000
VITE_BACKEND_IBC_THIAGO_WS_URL=ws://localhost:3001

# IBC Thiago Backend with Imported Data (Connected to user's MinIO/MongoDB)
# This backend uses the imported data from IBC_Thiago
# Note: This backend connects directly (no proxy needed for localhost)
VITE_BACKEND_IBC_THIAGO_IMPORTED_URL=http://localhost:3002
VITE_BACKEND_IBC_THIAGO_IMPORTED_WS_URL=ws://localhost:3003

# Upcoming Demo Backend (Custom API with limited feature set)
VITE_BACKEND_UPCOMING_DEMO_URL=https://upcoming-demo.example.com

# =============================================================================
# API CONFIGURATION
# =============================================================================
# Note: In production, the app automatically uses /api/proxy (Vercel proxy)
# In development, it uses /api (Vite dev server proxy)
# This variable is no longer needed but kept for backward compatibility
VITE_API_URL=http://34.216.9.25:8000

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
VITE_APP_TITLE=TAMS Demo
VITE_APP_DESCRIPTION=Time Addressable Media Storage Demo

# =============================================================================
# FEATURE FLAGS
# =============================================================================
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_SEARCH=true

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================
VITE_DEBUG_MODE=false

# =============================================================================
# PROXY CONFIGURATION
# =============================================================================
# The frontend automatically handles CORS by using proxies for certain backends:
# - vast-tams: Uses proxy (/api or /api/proxy) to avoid CORS issues with remote server
# - ibc-thiago-imported: Connects directly (no proxy needed for localhost)
# - Other backends: Connect directly unless they have CORS restrictions
#
# This is handled automatically in the API configuration - no manual setup required.

# =============================================================================
# BACKEND SWITCHING INSTRUCTIONS
# =============================================================================
# To switch backends:
# 1. Comment out the current VITE_DEFAULT_BACKEND line
# 2. Uncomment the desired backend line
# 3. Restart the development server
#
# Example - Switch back to VAST TAMS:
# VITE_DEFAULT_BACKEND=ibc-thiago
# VITE_DEFAULT_BACKEND=vast-tams
#
# Example - Switch to BBC TAMS Demo:
# VITE_DEFAULT_BACKEND=bbc-tams-demo
# # VITE_DEFAULT_BACKEND=ibc-thiago
#
# Example - Switch to IBC Thiago (current):
# VITE_DEFAULT_BACKEND=ibc-thiago
# # VITE_DEFAULT_BACKEND=vast-tams
#
# Example - Switch to IBC Thiago with Imported Data (current):
# VITE_DEFAULT_BACKEND=ibc-thiago-imported
# # VITE_DEFAULT_BACKEND=ibc-thiago
